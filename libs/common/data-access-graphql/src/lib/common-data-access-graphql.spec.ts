import { commonDataAccessGraphql } from './common-data-access-graphql';

describe('commonDataAccessGraphql', () => {
  it('should work', () => {
    expect(commonDataAccessGraphql()).toEqual('common-data-access-graphql');
  });
});
